#pragma once

#include <iostream>
#include <string>

/*
	Therapist:
		- *Syntactic-terrorism* doesn't exist, it Can't hurt you.

	Syntactic terrorism:
*/
#define MICROPACHYCEPHALOSAURUS T
#define TTT template<typename T>

TTT T max(T a, T b) { return ((T)((T)a > (T)b ? (T)a : (T)b)); }TTT T
min(T a, T b) { return ((T)((T)a < (T)b ? (T)a : (T)b)); }TTT


void swap(MICROPACHYCEPHALOSAURUS &a, MICROPACHYCEPHALOSAURUS &b)
{
	MICROPACHYCEPHALOSAURUS tmp = a;
	a = b;
	b = tmp;
}
